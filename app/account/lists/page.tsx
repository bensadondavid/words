
type FormList = {
  name : string,
  firstLanguage : string, 
  secondLanguage: string
}

export default function lists() {

  const form

  const addList = async()=>{
    const response = await 
  }

  return (
    <div></div>
  )
}
